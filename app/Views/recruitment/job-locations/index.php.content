<?php use App\Core\Auth; ?>
<?= flash_messages() ?>

<div class="card">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
      <h4 class="card-title mb-0">Job Locations</h4>
      <?php if (Auth::can('recruitment.manage')): ?>
        <a href="<?= url('/recruitment/job-locations/create') ?>" class="btn btn-info btn-sm waves-effect waves-light"><i class="mdi mdi-plus"></i> Add Job Location</a>
      <?php endif; ?>
    </div>

    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead>
          <tr><th>Name</th><th>Remote</th><th>City</th><th>Country</th><th>Status</th><?php if (Auth::can('recruitment.manage')): ?><th></th><?php endif; ?></tr>
        </thead>
        <tbody>
          <?php if (empty($locations)): ?>
            <tr><td colspan="6" class="text-center text-muted py-4">No job locations yet.</td></tr>
          <?php endif; ?>
          <?php foreach ($locations as $l): ?>
            <tr>
              <td><?= e($l['name']) ?></td>
              <td><?= !empty($l['remote_work']) ? 'Yes' : 'No' ?></td>
              <td><?= e($l['city'] ?: '—') ?></td>
              <td><?= e($l['country'] ?: '—') ?></td>
              <td><span class="badge bg-<?= $l['status'] === 'Active' ? 'success' : 'secondary' ?>"><?= e($l['status']) ?></span></td>
              <?php if (Auth::can('recruitment.manage')): ?>
              <td class="text-end">
                <a href="<?= url('/recruitment/job-locations/' . $l['id'] . '/edit') ?>" class="btn btn-sm btn-outline-secondary"><i class="mdi mdi-pencil"></i></a>
                <form method="post" action="<?= url('/recruitment/job-locations/' . $l['id'] . '/delete') ?>" class="d-inline" onsubmit="return confirm('Delete this job location?');">
                  <?= csrf_field() ?>
                  <button type="submit" class="btn btn-sm btn-outline-danger"><i class="mdi mdi-delete"></i></button>
                </form>
              </td>
              <?php endif; ?>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>
  </div>
</div>
