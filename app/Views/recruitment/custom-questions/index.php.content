<?php use App\Core\Auth; ?>
<?= flash_messages() ?>

<div class="card">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
      <h4 class="card-title mb-0">Application Questions</h4>
      <?php if (Auth::can('recruitment.manage')): ?>
        <a href="<?= url('/recruitment/custom-questions/create') ?>" class="btn btn-info btn-sm waves-effect waves-light" data-modal-url="<?= url('/recruitment/custom-questions/create') ?>" data-modal-title="Add Application Question" data-modal-size="md"><i class="mdi mdi-plus"></i> Add Application Question</a>
      <?php endif; ?>
    </div>

    <form method="get" action="<?= url('/recruitment/custom-questions') ?>" class="d-flex gap-2 mb-3" style="max-width:420px;">
      <input type="text" name="q" class="form-control" placeholder="Search questions..." value="<?= e($search) ?>">
      <button class="btn btn-success flex-shrink-0" type="submit"><i class="mdi mdi-magnify"></i> Search</button>
    </form>

    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead>
          <tr><th>Question</th><th>Type</th><th>Required</th><th>Active</th><th>Sort Order</th><?php if (Auth::can('recruitment.manage')): ?><th></th><?php endif; ?></tr>
        </thead>
        <tbody>
          <?php if (empty($questions)): ?>
            <tr><td colspan="6" class="text-center text-muted py-4">No application questions found.</td></tr>
          <?php endif; ?>
          <?php foreach ($questions as $q): ?>
            <tr>
              <td><?= e($q['question']) ?></td>
              <td><?= e($q['type']) ?></td>
              <td><span class="badge bg-<?= !empty($q['is_required']) ? 'success' : 'secondary' ?>"><?= !empty($q['is_required']) ? 'Yes' : 'No' ?></span></td>
              <td><span class="badge bg-<?= !empty($q['is_active']) ? 'success' : 'secondary' ?>"><?= !empty($q['is_active']) ? 'Yes' : 'No' ?></span></td>
              <td><?= e($q['sort_order']) ?></td>
              <?php if (Auth::can('recruitment.manage')): ?>
              <td class="text-end">
                <a href="<?= url('/recruitment/custom-questions/' . $q['id'] . '/edit') ?>" class="btn btn-sm btn-outline-secondary" data-modal-url="<?= url('/recruitment/custom-questions/' . $q['id'] . '/edit') ?>" data-modal-title="Edit Application Question" data-modal-size="md"><i class="mdi mdi-pencil"></i></a>
                <form method="post" action="<?= url('/recruitment/custom-questions/' . $q['id'] . '/delete') ?>" class="d-inline" onsubmit="return confirm('Delete this question?');">
                  <?= csrf_field() ?>
                  <button type="submit" class="btn btn-sm btn-outline-danger"><i class="mdi mdi-delete"></i></button>
                </form>
              </td>
              <?php endif; ?>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>
  </div>
</div>
