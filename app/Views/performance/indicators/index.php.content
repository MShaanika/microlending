<?php use App\Core\Auth; ?>
<?= flash_messages() ?>

<div class="card">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
      <h4 class="card-title mb-0">Performance Indicators</h4>
      <?php if (Auth::can('performance.manage')): ?>
        <a href="<?= url('/performance/indicators/create') ?>" class="btn btn-info btn-sm waves-effect waves-light" data-modal-url="<?= url('/performance/indicators/create') ?>" data-modal-title="Add Performance Indicator" data-modal-size="lg"><i class="mdi mdi-plus"></i> Add Indicator</a>
      <?php endif; ?>
    </div>

    <form method="get" action="<?= url('/performance/indicators') ?>" class="d-flex flex-wrap gap-2 align-items-center justify-content-between mb-3">
      <input type="hidden" name="sort" value="<?= e($sort) ?>">
      <input type="hidden" name="dir" value="<?= e($dir) ?>">
      <div class="d-flex gap-2 flex-grow-1" style="min-width:260px; max-width:420px;">
        <input type="text" name="q" class="form-control" placeholder="Search indicators..." value="<?= e($search) ?>">
        <button class="btn btn-success flex-shrink-0" type="submit"><i class="mdi mdi-magnify"></i> Search</button>
      </div>
      <select name="per_page" class="form-select form-select-sm" style="width:auto" onchange="this.form.submit()">
        <?php foreach ([10, 25, 50, 100] as $pp): ?>
          <option value="<?= $pp ?>" <?= $perPage === $pp ? 'selected' : '' ?>><?= $pp ?> per page</option>
        <?php endforeach; ?>
      </select>
    </form>

    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead>
          <tr>
            <?= sortable_th('Name', 'name', $_GET, $sort, $dir) ?>
            <?= sortable_th('Category', 'category', $_GET, $sort, $dir) ?>
            <th>Measurement Unit</th>
            <th>Target</th>
            <?= sortable_th('Status', 'status', $_GET, $sort, $dir) ?>
            <?php if (Auth::can('performance.manage')): ?><th class="text-end">Actions</th><?php endif; ?>
          </tr>
        </thead>
        <tbody>
          <?php if (empty($indicators)): ?>
            <tr><td colspan="6" class="text-center text-muted py-4">No performance indicators found.</td></tr>
          <?php endif; ?>
          <?php foreach ($indicators as $i): ?>
            <tr>
              <td><?= e($i['name']) ?></td>
              <td><?= e($i['category_name'] ?: '—') ?></td>
              <td><?= e($i['measurement_unit'] ?: '—') ?></td>
              <td><?= e($i['target_value'] ?: '—') ?></td>
              <td><span class="badge bg-<?= $i['status'] === 'Active' ? 'success' : 'secondary' ?>"><?= e($i['status']) ?></span></td>
              <?php if (Auth::can('performance.manage')): ?>
              <td class="text-end">
                <a href="<?= url('/performance/indicators/' . $i['id'] . '/edit') ?>" class="btn btn-sm btn-outline-secondary" data-modal-url="<?= url('/performance/indicators/' . $i['id'] . '/edit') ?>" data-modal-title="Edit Performance Indicator" data-modal-size="lg"><i class="mdi mdi-pencil"></i></a>
                <form method="post" action="<?= url('/performance/indicators/' . $i['id'] . '/delete') ?>" class="d-inline" onsubmit="return confirm('Delete this indicator?');">
                  <?= csrf_field() ?>
                  <button type="submit" class="btn btn-sm btn-outline-danger"><i class="mdi mdi-delete"></i></button>
                </form>
              </td>
              <?php endif; ?>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>

    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mt-2">
      <p class="text-muted small mb-0">Showing <?= count($indicators) ?> of <?= $total ?> result<?= $total === 1 ? '' : 's' ?></p>
      <?= pagination_nav($page, $totalPages, $_GET) ?>
    </div>
  </div>
</div>
