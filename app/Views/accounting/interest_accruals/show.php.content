<?= flash_messages() ?>

<div class="card">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h4 class="card-title mb-0">Interest Accrual Run &mdash; <?= e($accrualDate) ?></h4>
      <a href="<?= url('/accounting/interest-accruals') ?>" class="btn btn-outline-secondary btn-sm"><i class="mdi mdi-arrow-left"></i> Back</a>
    </div>

    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead><tr><th>Loan</th><th>Borrower</th><th class="text-end">Interest</th><th>Status</th></tr></thead>
        <tbody>
          <?php if (empty($lines)): ?>
            <tr><td colspan="4" class="text-center text-muted py-4">No lines found for this run.</td></tr>
          <?php endif; ?>
          <?php foreach ($lines as $line): ?>
            <tr>
              <td><a href="<?= url('/loans/' . $line['loan_id']) ?>"><?= e($line['loan_no']) ?></a></td>
              <td><?= e($line['borrower_name']) ?></td>
              <td class="text-end"><?= e(format_money($line['amount'])) ?></td>
              <td><span class="badge bg-<?= $line['status'] === 'Accrued' ? 'success' : 'secondary' ?>"><?= e($line['status']) ?></span></td>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>
  </div>
</div>
