<?php use App\Core\Auth; ?>
<?= flash_messages() ?>

<div class="card">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
      <h4 class="card-title mb-0">Leave Types</h4>
      <?php if (Auth::can('hrm.manage')): ?>
        <a href="<?= url('/hrm/leave-types/create') ?>" class="btn btn-info btn-sm waves-effect waves-light" data-modal-url="<?= url('/hrm/leave-types/create') ?>" data-modal-title="Add Leave Type" data-modal-size="md"><i class="mdi mdi-plus"></i> Add Leave Type</a>
      <?php endif; ?>
    </div>

    <form method="get" action="<?= url('/hrm/leave-types') ?>" class="d-flex flex-wrap gap-2 align-items-center justify-content-between mb-3">
      <input type="hidden" name="sort" value="<?= e($sort) ?>">
      <input type="hidden" name="dir" value="<?= e($dir) ?>">
      <div class="d-flex gap-2 flex-grow-1" style="min-width:260px; max-width:420px;">
        <input type="text" name="q" class="form-control" placeholder="Search leave types..." value="<?= e($search) ?>">
        <button class="btn btn-success flex-shrink-0" type="submit"><i class="mdi mdi-magnify"></i> Search</button>
      </div>
      <select name="per_page" class="form-select form-select-sm" style="width:auto" onchange="this.form.submit()">
        <?php foreach ([10, 25, 50, 100] as $pp): ?>
          <option value="<?= $pp ?>" <?= $perPage === $pp ? 'selected' : '' ?>><?= $pp ?> per page</option>
        <?php endforeach; ?>
      </select>
    </form>

    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead>
          <tr>
            <?= sortable_th('Name', 'name', $_GET, $sort, $dir) ?>
            <?= sortable_th('Max Days/Year', 'max_days_per_year', $_GET, $sort, $dir) ?>
            <th>Paid</th>
            <?= sortable_th('Status', 'status', $_GET, $sort, $dir) ?>
            <?php if (Auth::can('hrm.manage')): ?><th></th><?php endif; ?>
          </tr>
        </thead>
        <tbody>
          <?php if (empty($leaveTypes)): ?>
            <tr><td colspan="5" class="text-center text-muted py-4">No leave types yet.</td></tr>
          <?php endif; ?>
          <?php foreach ($leaveTypes as $lt): ?>
            <tr>
              <td><span class="badge" style="background-color: <?= e($lt['color']) ?>">&nbsp;</span> <?= e($lt['name']) ?></td>
              <td><?= (int) $lt['max_days_per_year'] ?></td>
              <td><?= $lt['is_paid'] ? 'Yes' : 'No' ?></td>
              <td><span class="badge bg-<?= $lt['is_active'] ? 'success' : 'secondary' ?>"><?= $lt['is_active'] ? 'Active' : 'Inactive' ?></span></td>
              <?php if (Auth::can('hrm.manage')): ?>
              <td class="text-end">
                <a href="<?= url('/hrm/leave-types/' . $lt['id'] . '/edit') ?>" class="btn btn-sm btn-outline-secondary" data-modal-url="<?= url('/hrm/leave-types/' . $lt['id'] . '/edit') ?>" data-modal-title="Edit Leave Type" data-modal-size="md"><i class="mdi mdi-pencil"></i></a>
                <form method="post" action="<?= url('/hrm/leave-types/' . $lt['id'] . '/toggle-active') ?>" class="d-inline">
                  <?= csrf_field() ?>
                  <button type="submit" class="btn btn-sm btn-outline-<?= $lt['is_active'] ? 'danger' : 'success' ?>">
                    <?= $lt['is_active'] ? 'Deactivate' : 'Activate' ?>
                  </button>
                </form>
              </td>
              <?php endif; ?>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>

    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mt-2">
      <p class="text-muted small mb-0">Showing <?= count($leaveTypes) ?> of <?= $total ?> result<?= $total === 1 ? '' : 's' ?></p>
      <?= pagination_nav($page, $totalPages, $_GET) ?>
    </div>
  </div>
</div>
