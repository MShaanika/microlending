<?php $d = $deduction; ?>
<?= flash_messages() ?>

<div class="card">
  <div class="card-body">
    <h4 class="card-title mb-3">Edit Deduction</h4>
    <p class="text-muted small"><?= e($d['employee_name']) ?> &middot; <?= e($d['type_name']) ?></p>

    <form method="post" action="<?= url('/hrm/deductions/' . $d['id']) ?>">
      <?= csrf_field() ?>

      <div class="row">
        <div class="col-md-6 mb-3">
          <label class="form-label">Type</label>
          <select name="type" class="form-select">
            <option value="Fixed" <?= $d['type'] === 'Fixed' ? 'selected' : '' ?>>Fixed Amount (NAD)</option>
            <option value="Percentage" <?= $d['type'] === 'Percentage' ? 'selected' : '' ?>>Percentage of Basic Salary</option>
          </select>
        </div>
        <div class="col-md-6 mb-3">
          <label class="form-label">Amount</label>
          <input type="number" step="0.01" name="amount" class="form-control" value="<?= e($d['amount']) ?>">
        </div>
      </div>

      <button type="submit" class="btn btn-info waves-effect waves-light"><i class="mdi mdi-content-save"></i> Save Changes</button>
      <a href="<?= url('/hrm/deductions') ?>" class="btn btn-outline-secondary">Cancel</a>
    </form>
  </div>
</div>
