<?php use App\Core\Auth; ?>
<?= flash_messages() ?>

<div class="card">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
      <h4 class="card-title mb-0">Designations</h4>
      <?php if (Auth::can('hrm.manage')): ?>
        <a href="<?= url('/hrm/designations/create') ?>" class="btn btn-info btn-sm waves-effect waves-light"><i class="mdi mdi-plus"></i> Add Designation</a>
      <?php endif; ?>
    </div>

    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead>
          <tr><th>Designation</th><th>Department</th><th>Branch</th><th>Status</th><?php if (Auth::can('hrm.manage')): ?><th></th><?php endif; ?></tr>
        </thead>
        <tbody>
          <?php if (empty($designations)): ?>
            <tr><td colspan="5" class="text-center text-muted py-4">No designations yet.</td></tr>
          <?php endif; ?>
          <?php foreach ($designations as $d): ?>
            <tr>
              <td><?= e($d['designation_name']) ?></td>
              <td><?= e($d['department_name'] ?: '—') ?></td>
              <td><?= e($d['branch_name'] ?: '—') ?></td>
              <td><span class="badge bg-<?= $d['is_active'] ? 'success' : 'secondary' ?>"><?= $d['is_active'] ? 'Active' : 'Inactive' ?></span></td>
              <?php if (Auth::can('hrm.manage')): ?>
              <td class="text-end">
                <a href="<?= url('/hrm/designations/' . $d['id'] . '/edit') ?>" class="btn btn-sm btn-outline-secondary"><i class="mdi mdi-pencil"></i></a>
                <form method="post" action="<?= url('/hrm/designations/' . $d['id'] . '/toggle-active') ?>" class="d-inline">
                  <?= csrf_field() ?>
                  <button type="submit" class="btn btn-sm btn-outline-<?= $d['is_active'] ? 'danger' : 'success' ?>">
                    <?= $d['is_active'] ? 'Deactivate' : 'Activate' ?>
                  </button>
                </form>
              </td>
              <?php endif; ?>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>
  </div>
</div>
